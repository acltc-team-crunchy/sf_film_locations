require 'spec_helper'

describe SfFilmLocations do
  it 'has a version number' do
    expect(SfFilmLocations::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
